import styled from 'styled-components';

const PortfolioTabsWrapper = styled.div`
  display: flex;
  align-items: flex-end;

  height: 50px;

  margin: 50px 0 0 0;
`;

export default PortfolioTabsWrapper;
